[project]
name = "redis-basic-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.2",
    "redis>=8.0.1",
    "supabase>=2.31.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
